export const intitialState = {
    item: "workout",
    completed: false, 
    id: 1
}

export function reducer(state, action){
    switch (action.type ){
        
    default: 
        return state;
    }
}